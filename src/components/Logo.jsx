import React from "react";

import logo from "../assets/logo.jpg";

const Logo = () => {
  return (
    <div>
      <img src={logo} alt="Logo" width={60} height={60} />
    </div>
  );
};

export default Logo;
